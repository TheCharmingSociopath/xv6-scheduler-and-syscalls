#include "types.h"
#include "stat.h"
#include "user.h"
 
int main(int argc, char *argv[])
{
    // sleep(10);
    while (1);
    // for (uint i = 0; i < 1e30; ++i)
    // {
        // printf(1, my)
        // printf(1, "sergsrgs\n");
    // }
    exit();
}
